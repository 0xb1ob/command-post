# beads16 fixture

Minimal schema-16 `.beads/` tracker for `test/br-contract.sh`. Generated once
with br 0.2.19 (`br init --prefix t`): 51 closed issues, one open labeled issue,
one blocker, one blocks dep, one artifact comment.

**Open issue id (artifact comment):** `t-z34`

Copy into `$TMP` per test run; never point tests at the live command-post home
tracker or operator backups.
