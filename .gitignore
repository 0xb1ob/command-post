# Personal / machine-local state (not tracked)
data/
projects/
.beads/
!test/fixtures/beads16/.beads/
.lavish/

# Runtime jobs map (state/jobs.tsv) / secrets
state/
bin/.cp-bin
.env
captain.md

# Agent-harness skill copies. Source of truth is tracked skills/.
# bin/install.sh copies skills/ into these dirs so Cursor, Claude Code, and
# Codex discover them. Never commit the copies.
.cursor/skills/
.claude/skills/
.agents/skills/
